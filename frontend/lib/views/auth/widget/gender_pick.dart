import 'package:flutter/material.dart';

class GenderPick extends StatelessWidget {
  final String gender; // 여성 남성

  const GenderPick({Key? key, required this.gender}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
