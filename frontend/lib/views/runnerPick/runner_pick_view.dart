import 'package:flutter/material.dart';

class RunnerPickView extends StatelessWidget {
  const RunnerPickView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
      child: Text('Runner Pick content goes here'), // 메인 콘텐츠
    ));
  }
}
